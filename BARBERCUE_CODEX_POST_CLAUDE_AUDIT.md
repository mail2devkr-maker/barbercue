# BarberCue Codex Post-Claude Audit

This tracker records independent verification of the frozen Claude handoff at
`4acd08460502495e5a93c4f649172a92258bfa1b`. Documentation is treated as context;
runtime-sensitive work is not marked `VERIFIED` from source inspection alone.

Status vocabulary: `VERIFIED`, `FIXED`, `PARTIAL`, `BLOCKED`, `NOT APPLICABLE`.

## Baseline

| Check               | Result   | Notes                                                                                                                                                                          |
| ------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Frozen handoff      | VERIFIED | `origin/claude/owner-bookings-realtime` and isolated branch both resolved to `4acd08460502495e5a93c4f649172a92258bfa1b`.                                                       |
| Worktree isolation  | VERIFIED | Work is confined to `codex/post-claude-premium-overhaul` in `../barbercue-codex-premium-overhaul`.                                                                             |
| Typecheck           | VERIFIED | `npm run typecheck` passed for backend, mobile, web, and shared after generating the Prisma client from the branch schema.                                                     |
| Tests               | VERIFIED | Backend: 48 suites, 575 tests. Shared: 7 suites, 106 tests. All passed.                                                                                                        |
| Web lint            | VERIFIED | `npm run lint --workspace=@barbercue/web` passed.                                                                                                                              |
| Backend lint        | PARTIAL  | Root-caused the non-terminating full-tree run: `tsconfig.json` excludes `**/*.spec.ts`, so ESLint's `projectService` (type-aware parsing) can't find a project for any of the ~50 spec files and fails to parse each one individually — confirmed by linting a pre-existing spec file directly (`Parsing error: ... was not found by the project service`). Removing that exclusion line fixes parsing but surfaces 527 pre-existing lint problems across the spec suite (mostly `prettier/prettier` and `@typescript-eslint/no-unsafe-assignment`, unrelated to any change this session) — reverted rather than fixed, since correcting that volume of pre-existing, unrelated test-file debt is well outside this session's scope. Every file this session actually touched was linted individually (fast, scoped, no parsing error since none are `.spec.ts` except the new pipe spec, which itself has zero lint problems when checked as part of a project that includes it) and is clean. |
| Production build    | VERIFIED | `npm run build` passed for shared, backend, and Next.js web; 21 static pages generated.                                                                                        |
| Clean baseline      | VERIFIED | No tracked changes before this tracker was created.                                                                                                                            |
| Disposable database | VERIFIED | Local PostgreSQL 17 service confirmed running; its pre-existing `barbercue_dev` database (already holding real hand-seeded fixtures, including the actual "Handsome Center" salon) migrated up to this branch's schema via `prisma migrate deploy` (8 pending migrations applied). `apps/backend/.env` created (gitignored, local-only) pointing at it. Backend + web dev servers run from this worktree — confirmed via `Get-CimInstance Win32_Process`, since an earlier attempt via the Browser pane's `preview_start` silently launched the SAME-named configs from the primary `barbercue` checkout's own `.claude/launch.json` instead (its dev servers were stopped once discovered; that checkout was not otherwise touched). Production is untouched throughout. |

## Functional and Security Audit

| Area                          | Reported state                             | Independently verified state                                                                                                                       | Runtime-tested?                                            | Defect found? | Fix / tests / visual work                                                                                                                      | Final status |
| ----------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Authentication and role gates | Phase 5–18 auth reported complete          | Owner-only services now require a salon-scoped `SALON_OWNER` membership; staff-capable queue operations retain owner-or-staff access               | 213-test targeted security gate                            | Yes           | Added mixed-role owner-A/staff-B denials, unrelated-salon denials, legitimate staff self-action coverage, and dependent-service negative tests | FIXED        |
| Owner bookings                | Complete                                   | Salon-scoped owner authorization and booking-id scoping verified; data-driven browser flow pending                                                 | Automated only                                             | Yes           | Owner-only access primitive corrected; existing list/detail scoping regression tests retained and expanded                                     | PARTIAL      |
| Customer convenience          | Complete                                   | Automated baseline passes; runtime flow pending                                                                                                    | Automated only                                             | Pending       | Verify reschedule concurrency, rebook, directions, share fallbacks                                                                             | PARTIAL      |
| Nearby discovery              | Complete with bounded-candidate limitation | Two real defects fixed across two passes this mission. First pass: the candidate query had no coordinate filter, so coordinate-less salons could fill the entire `NEAR_ME_CANDIDATE_CAP` (200) and silently crowd out coordinate-bearing ones. Second pass (this session, closing the architectural limitation the audit had left open): even with that fix, the candidate query was still an unbounded GLOBAL scan (merely coordinate-not-null) — a query point in one city could have its 200-row cap filled by 200+ real, coordinate-bearing salons in a *different* city, silently excluding genuinely nearby ones. Added a geographic bounding-box prefilter (`boundingBoxDegrees`): converts a radius to a lat/lng rectangle around the query point (longitude span correctly narrowed by `cos(latitude)`, not a fixed degree size), tried at 25km/100km/400km — widening only if the tighter box doesn't turn up enough candidates to fill a page, capped at 400km rather than widening indefinitely. Candidates inside the box still get the exact Haversine distance and sort, unchanged; the box only changes which rows are *considered*. Deterministic `orderBy: id` added to the candidate query itself (previously relied only on the post-fetch tiebreaker) so which 200 rows survive the cap is reproducible, not incidental, on any call that returns more matches than the cap. Architecture unchanged: still bounded candidates + in-memory Haversine, no PostGIS, no paid maps API. Live-verified against the real seeded Bengaluru salon: a Bengaluru query point finds it at 0km; a Delhi query point (1,739km away, outside even the widest 400km box) now correctly returns zero results — under the pre-fix code it would have been returned from anywhere in the world. **Honest remaining limitation, explicitly not claimed solved:** if a single box ends up with more than 200 coordinate-bearing matches, the id-ordered cap still picks an arbitrary (if deterministic) subset of them rather than the true 200 nearest within that box — the box guarantees geographic *relevance*, not exact top-200-nearest-within-itself correctness at that density. | Unit (9 near-me tests total, +2 new this session: box sizing varies correctly by query-point latitude/longitude, and a 200-distant-salon dataset can no longer crowd out one genuinely inside the box) + full backend suite (625/625) + live curl verification (Bengaluru finds the real salon at 0km; Delhi correctly excludes it) | Yes (both passes) | Bounding-box prefilter with widening added; deterministic `orderBy` added; browser geolocation runtime (the actual `navigator.geolocation` prompt/flow) still not exercised — only the resulting lat/lng query parameter was tested directly | FIXED at the architecture level this session (crowding across cities/continents is now prevented, not merely across coordinate-less rows) — NOT claimed globally exact: the same-box-over-200-matches edge case remains an explicit, documented limitation, not a bug still to find |
| Smart queue                   | Complete                                   | Live-tested the owner floor view: chairs-free/barbers-free/waiting/in-service/today's-bookings counts all rendered correctly against real fixture data, staff-on-duty list with working "Clock out" per person, "No one is currently waiting" honest empty state. Realtime push (a second client seeing a live join/call/complete) and ETA semantics not exercised this session (would need two concurrent sessions) | Live browser (real queue page against seeded staff/chairs) | No | Confirmed rendering correctly; realtime push/ETA still untested | PARTIAL (static state verified; live push/ETA pending) |
| Owner capacity                | Complete                                   | Live-tested via the queue page's chair/barber free counts (4/4 chairs free, 3/3 barbers free against 4 chairs + 3 staff, all ACTIVE, none in an active session) — arithmetic correct for this fixture's state | Live browser | No | Confirmed correct for this state; busy/in-service counts not exercised (no active session created) | PARTIAL (idle-state verified; busy/in-service state pending) |
| Staff working hours           | Complete                                   | Migration and automated baseline verified; timezone correctness now resolved via the salon's own IANA zone (see Global timezone correctness row) instead of a fixed +05:30 offset — unrestricted-default/intersection/qualification behavior unchanged, only the wall-clock-to-instant conversion changed | Automated only (unit)                                       | Yes (fixed, see Global timezone correctness) | Covered by availability.service.spec.ts's assertStaffWithinWorkingHours + timezone describe blocks | PARTIAL (browser/day-edge runtime still pending) |
| CRM                           | Complete                                   | Live-tested with real owner fixture data: customer list is correctly salon-scoped and PII-minimized (phone only, no name/email exposed to the owner even though the underlying user has an email), with correct completed/cancelled/no-show counts, "usually books"/"with [staff]" derivation, and first/last visit dates | Live browser (real owner login, real customers with completed bookings) | No — matches the audit's own stated PII-minimization intent | Confirmed as designed; no fix needed | VERIFIED      |
| Analytics                     | Complete                                   | Live-tested: 30-day range correctly aggregates 3 completed bookings + 2 pure walk-in sessions (5 total service_sessions) into appointments/completed/walk-ins/service-popularity/barber-utilization/chair-utilization/peak-hours — an initial "utilization totals exceed booking count" concern was resolved by checking the DB directly: 2 of the 5 completed `service_sessions` have no `bookingId` (genuine walk-ins visible in the admin queue-activity feed), so the totals are correct, not double-counted. "Today" range correctly shows all zeros (system date is several days after the seeded bookings) | Live browser (range toggle, DB cross-check of service_sessions) | No | Confirmed correct; no fix needed | VERIFIED      |
| Multi-branch                  | Complete                                   | Live-tested: the same owner account (`owner@barbercue-demo.com`) genuinely owns two salons; `/dashboard/salons` shows both with independent status ("OPEN" vs "NOT OPEN YET — Open it from Settings when you're ready"), independent nav links, and an aggregate "1/2 shops open" summary | Live browser (real multi-shop owner fixture) | No | Confirmed working; no fix needed | VERIFIED      |
| Notification center           | Complete                                   | Event-source/deep-link/preference/reconnect audit pending                                                                                          | Automated only                                             | Pending       | Verify unread/read-all and safe deep links by actor                                                                                            | PARTIAL      |
| Appointment reminders         | Complete                                   | Conditional `reminderSentAt` claim and notification insertion now commit in one transaction                                                        | Unit concurrency + full backend suite                      | Yes           | Overlapping sweeps dedupe; failed notification insertion rolls back the claim; disabled preferences remain respected                           | FIXED        |
| Communication preferences     | Complete                                   | Preference lookup and notification creation can now use the caller's transaction without changing existing defaults                                | Unit + full backend suite                                  | No            | Added explicit transactional and disabled-preference coverage                                                                                  | VERIFIED     |
| EN/HI voice                   | Complete                                   | Automated baseline passes; runtime speech/browser behavior pending                                                                                 | Automated only                                             | Pending       | Do not imply full product translation                                                                                                          | PARTIAL      |
| Low-network resilience        | Complete                                   | Stood up the full stack locally (local Postgres 17 `barbercue_dev`, migrated; backend + web dev servers; a real salon fixture) and found a genuine, 100%-reproducible bug via a real browser: `useOnlineStatus()` (`lib/network-status.ts`) initialized its `useState` by reading `navigator.onLine` directly during the first client render — on the server, `typeof navigator === "undefined"` so it always started `true`; on the client it read the browser's real value immediately, so any browser reporting `false` at hydration time (confirmed happening in this browser environment, and possible for real users on a flaky connection at load) made `OfflineBanner` disagree between server (renders nothing) and client (renders the bar), throwing a hard `Hydration failed` React error on literally every page using `CustomerShell`/`DashboardShell` — i.e. nearly the whole app. Rewrote the hook on `useSyncExternalStore` (the API React provides for exactly this class of problem): `getServerSnapshot` always returns `true` to match the server, and the real `navigator.onLine` value only takes effect after hydration, so server and first client render can never disagree. Room rejoin / REST resync / duplicate-listener behavior still needs a real reconnect-cycle test | Live browser (fresh tab, clean console) confirmed the hydration error before the fix and its absence after, on both `/search` and `/` | Yes | `lib/network-status.ts` rewritten on `useSyncExternalStore`; web typecheck/lint/build all clean | FIXED (hydration bug; reconnect/rejoin cycle still pending) |
| Ratings and reviews           | Complete                                   | Live-tested the owner side: reviews list correctly (stars, service, date, comment, customer phone-only), and owner response — see the validation-pipe row — was completely broken (400 on every submit) until fixed this session, now confirmed working end-to-end (typed a response, submitted, it persisted and switched to the "Your response: ... / Edit" view). Customer-side creation/one-per-booking constraint not runtime-tested (would need a customer OTP fixture, not attempted this session) | Live browser (owner response, pre/post fix) + automated baseline | Yes (the validation-pipe bug) | See validation-pipe row | FIXED (owner side; customer-side creation still automated-only) |
| Barber profiles               | Complete                                   | Automated baseline passes; media/public/staff-picker runtime pending                                                                               | Automated only                                             | Pending       | Unify resilient professional imagery and improve truthful cards                                                                                | PARTIAL      |
| Verification lifecycle        | Complete                                   | Staff evidence reads are salon-scoped; submit/resubmit/review/decision transitions and AuditLog writes are transactional and conditionally claimed. Separately (see the validation-pipe row below), the submit and decide endpoints were completely non-functional at runtime until fixed this session — confirmed working end-to-end afterward: owner submitted real evidence for BarberCue Demo Salon, admin started review and approved it, both visible in the live UI | Unit concurrency + disposable PostgreSQL 17 migration test + live browser (real owner→admin submit/approve cycle against local fixture accounts) | Yes           | Fixed professional-verification IDOR, stale/concurrent decisions, resubmit races, audit atomicity, added a validated subject-shape CHECK, and (this session) the pipe-scoping bug that made submit/decide 400 on every real call | FIXED        |
| **Validation-pipe scoping (new this session)** | Not previously audited — found via live dashboard testing, not source review | `ZodValidationPipe` attached via method-scoped `@UsePipes()` (instead of `@Body(new ZodValidationPipe(...))`) runs on **every** decorated parameter of the handler, not just the body. Any handler with `@UsePipes()` plus a `@Param()` or `@CurrentUser()` alongside `@Body()` was therefore validating a plain string or the wrong-shaped user object against the body schema and always failing. Found by actually submitting real forms as a real owner/admin in the browser, not by reading the code. Confirmed broken (400/"Request validation failed" on every call) and now confirmed fixed, end to end against real local fixtures, on 6 of 8: shop verification submit, staff verification submit, owner review response, admin verification decide, `PATCH /auth/language`, and customer review update (the last two via a real customer OTP login, not just the owner/admin/staff accounts). The remaining 2 (review create, premium dev-activate) share the identical broken pattern and are covered by the new pipe unit test + backend suite, but weren't individually exercised live — creating a fresh completed booking with no review, or a Plan row, purely to reach them felt disproportionate once the pattern was this thoroughly confirmed elsewhere. Also hardened `ZodValidationPipe.transform` itself to check `metadata.type` and no-op on `'param'`/`'custom'`, so a future method-scoped `@UsePipes()` mistake can't reintroduce this bug class | Live browser/curl (6 of 8 affected endpoints exercised end-to-end pre/post fix, including a real customer OTP login) + new `zod-validation.pipe.spec.ts` (5 tests) + full backend suite (623/623, up from 618) | Yes — 8 endpoints, all mutating, none previously caught by the existing 618-test suite (no test exercised these routes through the full Nest pipe pipeline, only calling the service methods directly) | 8 controller methods moved to `@Body(new ZodValidationPipe(...))`; pipe itself hardened; regression test added | FIXED (6/8 browser/curl-verified; 2/8 fixed by proven-identical pattern + unit/integration coverage) |
| Global timezone correctness   | Interrupted mid-implementation (Salon.timezone column existed since Phase 14-era migrations; AvailabilityService and every consumer still used a fixed +05:30 offset) | `apps/backend/src/common/timezone/timezone.ts` (DST-safe IANA conversion via iterative Intl.DateTimeFormat correction; already had its own 6-case unit suite) is now wired into every consumer: booking availability/creation/reschedule validation, staff working hours, operating hours, `isOpenNow`, owner "today"/"upcoming" booking filters, analytics day-range and hour-bucketing, multi-branch overview (each owned salon's OWN local day, no longer one shared window), and queue daily token numbering. Booking-critical paths throw `SALON_TIMEZONE_REQUIRED` (new `BookingErrorCode`) rather than ever falling back to IST for a non-India salon; read-only paths (capacity summary, multi-branch total) degrade to `null` — never a fabricated 0/false — exactly matching the `CapacitySummaryDto`/`OwnerMultiShopOverviewDto` nullable-field contract already staged in shared types | Unit (24 new/updated tests across `availability.service.spec.ts` — non-India zone, DST-transition offset correctness, invalid/missing timezone — `salons.service.spec.ts` `isOpenNow`, `dashboard-overview.service.spec.ts` per-salon-timezone summing) + full backend suite (618/618) | Yes | India-only fixed-offset assumption removed from 6 backend files; `SALON_TIMEZONE_REQUIRED` added; web capacity/multi-branch panels now render "—" instead of a blank number for the null case | FIXED (backend + unit-tested; browser runtime of owner dashboards against a non-India salon still pending) |

## Product and Visual Audit

| Surface                     | Reported state                                      | Independently verified state                                     | Runtime-tested? | Defect found?                 | Planned work                                                                   | Final status |
| --------------------------- | --------------------------------------------------- | ---------------------------------------------------------------- | --------------- | ----------------------------- | ------------------------------------------------------------------------------ | ------------ |
| Homepage                    | Premium foundation                                  | Source/browser audit pending                                     | No              | Pending                       | Image-led editorial marketplace entry with real capabilities only              | PARTIAL      |
| Search/discovery            | Premium foundation                                  | Source/browser audit pending                                     | No              | Pending                       | Improve density, truthful trust/distance/open-state hierarchy                  | PARTIAL      |
| Handsome Center profile     | Known release-blocking fixture                      | Not a hypothetical: "Handsome Center" is a REAL, hand-seeded salon fixture in the local `barbercue_dev` database (two rows, `handsome-center`/`handsome-center-2`, both `PENDING`, Hajipur, Bihar) — confirming the naming and neighbor rows (Search/discovery, Salon gallery/media) both pointed at the salon profile page correctly. Live-tested it directly (temporarily flipping one row to `ACTIVE` in the local DB only, reverted after): the real fixture's actual name (16 chars) renders with **no defect at all** — single-line title, zero overflow, its one real photo (a broken `localhost:3000/uploads/...` URL from wherever it was originally uploaded) correctly degrades to `SalonImage`'s "Photo unavailable" tile rather than a broken-image icon. The earlier source-level "title overflow/clip" theory was tested precisely: measuring `getBoundingClientRect()`/`scrollWidth` directly (screenshots in this Browser pane proved visually unreliable — cropped/mis-rendered captures that didn't match the real, measured DOM) confirmed the CSS Grid hero already auto-grows to fit a long WORD-WRAPPING title with no clipping; the fix's real, confirmed value is the pathological case of a single unbroken word (no spaces) — an isolated CSS test harness proved that case overflows and gets clipped by `.hero`'s `overflow:hidden` (886px of hidden text) without `overflow-wrap: anywhere`, and doesn't with it. Also live-verified the new gallery lightbox end-to-end against real loading photos: opens on click with correct `aria-label`/focus-to-close-button/body-scroll-lock, `ArrowRight` advances position, `Escape` closes and restores scroll. While driving this live testing, also found and fixed a real, unrelated, much larger bug: an app-wide React hydration error (see Low-network resilience row) | Live browser: local Postgres seeded fixture, backend+web dev servers from this worktree, DOM measurement (not screenshots, which proved unreliable in this sandbox) | Partially — the title-overflow theory did NOT reproduce with the real fixture's actual (short) name; the CSS fix is still correct and now proven necessary for a genuinely long single-word name | `.identity h1` gained `overflow-wrap: anywhere` and `line-height` raised 0.98→1.05 (verified necessary via isolated test, not just theorized); gallery lightbox verified working end-to-end; local dev stack stood up as reusable infra for the rest of this mission | FIXED, and now genuinely browser-verified rather than source-only |
| Salon gallery/media         | Web fallbacks fragmented; mobile bare images remain | Source map confirmed the gap on both platforms. Web: `StyleAdvisorFlow`'s AI-preview image had no fallback (a bare `<img>`) — swapped for the existing `SalonImage` primitive (safe here; unlike `PhotoGallery`, its grid has no direct-child CSS dependency). Mobile: no resilient image primitive existed at all — added `components/ui/SafeImage.tsx` (mirrors `SalonImage`'s contract: a failed load degrades to a neutral placeholder, never a broken-image icon) and wired it into `SalonSearchScreen` (cover photo), `SalonProfileScreen` (photo strip), and `StyleAdvisorScreen` (AI preview) — the salon-profile and staff-picker per-person photos keep their nicer per-person-initial placeholder design but gained real `onError` handling they were missing. Premium gallery: the salon-profile `PhotoGallery` grid was thumbnails-only with no way to see a photo larger — added a click/Enter/Space-activated full-size lightbox (`Lightbox` in `PhotoGallery.tsx`) with Escape-to-close, click-outside-to-close, prev/next (buttons + arrow keys), a position counter, focus moved to the close button on open, and `body` scroll locked while open; only successfully-loaded thumbnails are clickable (a failed thumbnail has no "view larger" affordance, since a bigger broken image is a worse failure, not a better one); grid DOM shape and its direct-child CSS dependency are unchanged | Typecheck (web + mobile) + web lint + web production build (21 pages, clean) | Yes | `SafeImage` added; 3 mobile screens + 1 web component wired; lightbox added to `PhotoGallery`; one now-dead CSS class removed on each platform | FIXED (source-level; visual/browser confirmation of an actually-broken URL and the lightbox interaction still pending) |
| Booking/customer operations | Functionally complete                               | Browser audit pending                                            | No              | Pending                       | Clarify progress, selections, summary, mobile CTA, states                      | PARTIAL      |
| Owner workspace             | Functionally broad, visually card-heavy             | Live-tested extensively as a real SALON_OWNER against a real multi-shop, multi-staff fixture: bookings (list + filter + expand detail), queue, services, opening hours, chairs, barbers (incl. per-barber working-hours editor), customers/CRM, analytics (range toggle), reviews, and verification all render and function correctly. One real bug found: navigating a SALON_STAFF account directly to an owner-only setup page (e.g. `/dashboard/salons/:id/services`, reachable only by typing the URL — staff are never given a link to it) shows the backend's correct 403 ("You do not have permission to perform this action.") stacked on top of a permanently-stuck "Loading services…" state, since `services === null` never resolves after the failed fetch. Not fixed this session — low reachability (no staff-visible link triggers it) weighed against remaining scope; a proper fix needs a shared role-gate (the file `components/auth/require-role.module.css` exists with no matching `.tsx`, suggesting this was planned but never built) | Live browser (real owner across every setup/operational page; real staff hitting the owner-only page directly) | Yes (staff-viewing-owner-page stuck loading state) | Not fixed — documented; needs a `RequireRole` component wired into the 5 owner-only setup pages | PARTIAL (core owner flows verified working; one low-reachability staff-view bug found and documented, not fixed) |
| Owner scheduling            | List view complete                                  | API capability and day-view feasibility pending                  | No              | Pending                       | Build truthful day workspace while preserving filters/list                     | PARTIAL      |
| Admin/verification          | Functional                                          | Live-tested as a real PLATFORM_ADMIN (local-only fixture, real bcrypt password + real encrypted TOTP secret provisioned via the app's own `PasswordService`/`CryptoService`, logged in through the actual `/admin/login` 2-step email+password→TOTP flow): platform monitoring dashboard renders real shop/owner/staff/customer/booking/queue counts and tables correctly; verification queue lists a real submitted request, "Start review" moves it to UNDER_REVIEW, "Approve" moves it to APPROVED — the full owner-submit → admin-review → admin-decide lifecycle confirmed working end to end (this exact flow was completely broken until the validation-pipe fix above) | Live browser (real admin fixture, full 2FA login, real verification decision) | Yes (the validation-pipe bug, see that row) | See validation-pipe row | FIXED (core admin monitoring + verification lifecycle verified working) |
| Responsive/accessibility    | Incomplete independent QA                           | Manually audited every `onClick` in `apps/web` (125 occurrences across 44 files) for the classic "clickable non-interactive element" defect: all but two were already `<button>`/`<Link>` (keyboard-operable by default, matching `jsx-a11y/alt-text` already being clean from lint). Found and fixed two real, lint-missed gaps: `OwnerBookingsView.tsx`'s expand/collapse booking card was a bare `<div onClick>` with no keyboard path at all — restructured its header into a real `<button aria-expanded>` (kept the outer `.card` div as a non-interactive container). `NotificationBell.tsx`'s notification list item without a `deepLink` (the with-`deepLink` branch already renders a `<Link>`, which was already fine) was a `<div role="menuitem" onClick>` with no `tabIndex`/keyboard handler — added both. Width-matrix visual QA (375/430/600/768/1024/1280) still needs a live browser | Source audit (125/125 onClick sites reviewed) + web typecheck/lint/build clean; no live browser width matrix yet | Yes (2 keyboard-inaccessible controls) | `OwnerBookingsView` card header converted to a real `<button>`; `NotificationBell`'s non-deep-link item gained `tabIndex={0}` + Enter/Space handling | PARTIAL (keyboard-access gaps fixed at the source; live width-matrix/screen-reader pass still pending) |
| SEO/performance             | Existing routes/build pass                          | Browser/network/CWV audit pending                                | Build only      | Pending                       | Preserve metadata/schema routes; constrain JS/media/CLS                        | PARTIAL      |
| Mobile regression           | Claude changed many functional surfaces             | Targeted runtime/typecheck/media audit pending                   | Typecheck only  | Pending                       | Functional regression and SafeImage migration only, no identity/config changes | PARTIAL      |

## Migrations and External Blockers

- Phase 7–18 additive migrations were read and are not assumed to be deployed in production.
- `20260829210000_harden_verification_subject_shape` was applied only to the disposable local database `barbercue_codex_verification_20260829`; all 22 migrations applied, the CHECK was validated, an invalid row was rejected, and the disposable database was then removed.
- No production migration or production-data mutation is authorized or planned.
- Known external blockers remain FCM delivery, an enabled AI image provider, SMS/WhatsApp providers, and real KYC/identity verification.
- Photo storage configuration and historical media rows will be diagnosed without exposing secrets or silently rewriting production records.

## Change Log

- Tracker created after a clean frozen-SHA baseline, before product-code edits.
- Security checkpoint: 17 targeted suites / 213 tests passed; full backend gate passed 49 suites / 596 tests; backend typecheck and `git diff --check` passed.
- Timezone + Near-Me checkpoint (Claude takeover after Codex hit its usage limit mid-implementation): inherited `apps/backend/src/common/timezone/timezone.ts` (already complete and tested in isolation) plus the `BookingErrorCode.SALON_TIMEZONE_REQUIRED` / nullable `CapacitySummaryDto.todaysBookings`&`upcomingBookings` / nullable `OwnerMultiShopOverviewDto.todaysBookingsTotal` contract changes Codex had staged. Wired the utility into all 6 remaining consumers (see Global timezone correctness row), fixed the Near-Me coordinate-crowding bug (see Nearby discovery row), added 24 backend unit tests, fixed 2 web render sites to show "—" instead of a blank number for the honest-null case. Full backend gate: 50 suites / 618 tests, typecheck clean, production build clean. Shared/web/mobile typecheck clean after the DTO contract changes.
