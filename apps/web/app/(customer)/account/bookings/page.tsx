// Customer account — my bookings. Placeholder; auth-gating arrives via middleware.ts in a later
// phase (Phase 1 explicitly excludes complete authentication).
export default function MyBookingsPage() {
  return (
    <main style={{ padding: "3rem 1.5rem" }}>
      <h1>My bookings</h1>
      <p>Customer account area — placeholder, not yet implemented.</p>
    </main>
  );
}
